<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Health Score - StayHealthy</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4CAF50; /* A fresh green */
            --secondary-color: #2196F3; /* A calming blue */
            --text-dark: #333333;
            --text-light: #666666;
            --bg-light: #F9F9F9; /* Used for card backgrounds */
            --bg-white: #FFFFFF; /* Body background */
            --border-color: #E0E0E0; /* Light borders */
        }

        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            line-height: 1.6;
            color: var(--text-dark);
            background-color: var(--bg-white); /* White background */
            display: flex; /* Use flexbox for vertical centering */
            flex-direction: column;
            min-height: 100vh; /* Ensure full viewport height */
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            flex-grow: 1; /* Allow container to grow and push footer down */
            display: flex;
            flex-direction: column;
            justify-content: center; /* Center content vertically */
            align-items: center; /* Center content horizontally */
            text-align: center;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            border-bottom: 1px solid var(--border-color);
            margin-bottom: 40px; /* Adjust margin for score page */
            width: 100%; /* Ensure header spans full width within container */
            max-width: 1000px; /* Match global container width for header if desired */
        }

        .logo {
            font-size: 1.8em;
            font-weight: 700;
            color: var(--primary-color);
            text-decoration: none;
        }

        nav a {
            text-decoration: none;
            color: var(--text-light);
            margin-left: 30px;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        nav a:hover {
            color: var(--primary-color);
        }

        /* Score Card Styling */
        .score-card {
            background-color: var(--bg-light);
            padding: 60px 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            margin-bottom: 60px; /* Space before footer if flex-grow not used on container */
            border: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .score-card h1 {
            font-size: 2.8em;
            color: var(--text-dark);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .score-card h2 {
            font-size: 6em; /* Large score display */
            color: var(--primary-color); /* Green for the score */
            font-weight: 700;
            line-height: 1;
            margin-top: 0;
            margin-bottom: 30px;
            padding: 10px 20px;
            background-color: rgba(76, 175, 80, 0.08); /* Light green background for score */
            border-radius: 10px;
            display: inline-block; /* To make background fit content */
        }

        .score-card p {
            font-size: 1.2em;
            color: var(--text-light);
            max-width: 500px;
            margin-top: 20px;
        }

        .btn {
            display: inline-block;
            background-color: var(--secondary-color); /* Blue button for next steps */
            color: var(--bg-white);
            padding: 15px 35px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1em;
            transition: background-color 0.3s ease, transform 0.2s ease;
            border: none;
            cursor: pointer;
            margin-top: 30px;
        }

        .btn:hover {
            background-color: #1976D2; /* Slightly darker blue */
            transform: translateY(-2px);
        }


        /* Footer */
        footer {
            text-align: center;
            padding: 30px 0;
            border-top: 1px solid var(--border-color);
            color: var(--text-light);
            font-size: 0.9em;
            width: 100%;
        }

        footer a {
            color: var(--text-light);
            text-decoration: none;
            margin: 0 10px;
        }

        footer a:hover {
            color: var(--primary-color);
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            header {
                flex-direction: column;
                text-align: center;
                margin-bottom: 20px;
            }

            nav {
                margin-top: 20px;
            }

            nav a {
                margin: 0 15px;
            }

            .score-card {
                padding: 40px 20px;
            }

            .score-card h1 {
                font-size: 2.2em;
            }

            .score-card h2 {
                font-size: 4em;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 15px;
            }

            .logo {
                font-size: 1.5em;
            }

            nav a {
                margin: 0 8px;
            }

            .score-card {
                padding: 30px 15px;
            }

            .score-card h1 {
                font-size: 1.8em;
            }

            .score-card h2 {
                font-size: 3em;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <a href="#" class="logo">StayHealthy</a>
            <nav>
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Program</a>
                <a href="#">Contact</a>
            </nav>
        </div>
    </header>

    <div class="container">
        <div class="score-card">
            <h1>Your health profile score</h1>
            <h2>85</h2>
            <p>Based on your responses, here is an initial assessment of your health profile.</p>
	    <p>Feedback: Although you are doing slightly better, don't rest on your laurels. Please make an effort to change your dietary habits. </p> 
            <p>Please report your score back!</p>
            <a href="#" class="btn">Explore Recommendations</a>
        </div>
    </div>

    <footer>
        <div class="container">
            <p>&copy; 2025 StayHealthy. All rights reserved.</p>
            <div>
                <a href="#">Privacy Policy</a>
                <a href="#">Terms of Service</a>
            </div>
        </div>
    </footer>
</body>
</html>